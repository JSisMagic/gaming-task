const BASE_URL = "http://localhost:5173"

const PATHS = {
  MACHINES_LIST: "/machines-list",
  SELECTED_MACHINE: "/selected-game",
}

const OUTCOMES = {
  LOSS: 0,
  WIN: 1,
}

export { BASE_URL, PATHS, OUTCOMES }
